Flask>=3.0,<4.0
pypdf>=5.0,<6.0
reportlab>=4.0,<5.0
Pillow>=10.0,<12.0

# Moteur HTML -> PDF : nécessite le binaire système wkhtmltopdf (pas un
# paquet pip). Voir le README pour l'installation selon votre OS.
